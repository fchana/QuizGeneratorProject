import { TimeConvertorPipe } from './time-convertor.pipe';

describe('TimeConvertorPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeConvertorPipe();
    expect(pipe).toBeTruthy();
  });
});
